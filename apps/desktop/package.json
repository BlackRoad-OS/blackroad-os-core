{
  "name": "@blackroad/desktop",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "lint": "echo 'No JS/TS files to lint in Tauri desktop app'",
    "test": "vitest",
    "postbuild": "echo 'desktop postbuild stub'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0-rc.14",
    "typescript": "^5.6.2",
    "vite": "^7.3.1",
    "vitest": "^2.0.5"
  }
}
